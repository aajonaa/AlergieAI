# RENAME THIS FILE TO .env.local

# GPU API Server URL
# Point this to your local vLLM or other OpenAI-compatible API server
NEXT_PUBLIC_API_URL=http://localhost:8000/v1

# NextAuth Configuration
# NEXTAUTH_URL should be the canonical URL of your site
NEXTAUTH_URL=http://localhost:3000

# NEXTAUTH_SECRET is used to encrypt the NextAuth.js JWT
# Generate a new secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-change-in-production

# For Docker deployment with host machine GPU API:
# Windows/Mac Docker Desktop: http://host.docker.internal:8000/v1
# Linux with host network: http://localhost:8000/v1
# Linux without host network: http://172.17.0.1:8000/v1 (docker0 bridge IP)

